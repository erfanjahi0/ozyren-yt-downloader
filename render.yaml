services:
  - type: web
    name: ozyren-yt-downloader
    runtime: image
    repo: https://github.com/YOUR_USERNAME/ozyren-yt-downloader
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
